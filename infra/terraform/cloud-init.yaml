#cloud-config

package_update: true
package_upgrade: true

packages:
  - git
  - ca-certificates
  - curl
  - gnupg
  - lsb-release

runcmd:
  - mkdir -p /etc/apt/keyrings
  - curl -fsSL https://download.docker.com/linux/ubuntu/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
  - echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" > /etc/apt/sources.list.d/docker.list
  - apt-get update
  - apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
  - systemctl enable docker
  - systemctl start docker
  - usermod -aG docker azureuser

  - rm -rf /opt/open-data-ai-analytics
  - git clone ${repo_url} /opt/open-data-ai-analytics
  - cd /opt/open-data-ai-analytics && docker compose up -d --build

  - echo "Docker project deployed successfully on port ${web_port}" > /opt/open-data-ai-analytics/deploy-status.txt